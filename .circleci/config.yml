version: 2.1
jobs:
  chromatic-deployment:
    docker:
      - image: circleci/node:14.21.0
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run: npm run install
      - run: npm run chromatic --project-token=${CHROMATIC_PROJECT_TOKEN}

workflows:
  chromatic-deploy:
    jobs:
      - chromatic-deployment